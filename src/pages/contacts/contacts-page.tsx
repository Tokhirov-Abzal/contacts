import { Header, MainWrapper } from "src/components";
import { Contacts } from "src/containers";

export const ContactsPage = () => {
  return (
    <MainWrapper>
      <Header />

      <Contacts />
    </MainWrapper>
  );
};
